node_modules/
.next/
.open-next/
.wrangler/
out/
build/
coverage/
test-results/
playwright-report/
blob-report/

package-lock.json
next-env.d.ts
cloudflare-env.d.ts
*.tsbuildinfo

# Owner-supplied private research is never reformatted (section 56).
research/private/

# Raw evidence artefacts are kept byte-for-byte as they were served, so that a
# saved page remains usable as evidence of what a source actually said.
docs/research/evidence/

scratch/
worker-configuration.d.ts

# Per-machine editor state, written by the tool rather than by a person. It is
# git-ignored, so it can never be committed — but prettier still read it and
# failed the format gate on a file nobody can fix in a commit.
.claude/settings.local.json
